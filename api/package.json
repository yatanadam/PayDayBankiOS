{
  "name": "mock-api",
  "version": "1.0.0",
  "description": "Mock API for testing front-end apps",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lowdb": "^1.0.0"
  },
  "devDependencies": {
    "json-server": "^0.15.1"
  }
}
